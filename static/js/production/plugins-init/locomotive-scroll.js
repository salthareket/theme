function init_locomotive_scroll(){
	var token_init = "locomotive-scroll-init";
    /*if($("[data-scroll]").length>0){
         const scroll = new LocomotiveScroll({
		      el: document.querySelector('[data-scroll-container]'),
	          smooth: true,
	          //getSpeed: true,
	          //getDirection: true,
	          multiplier: 1, // Scroll hız çarpanı
              //lerp: 0.1, // Smooth scroll yumuşaklık ayarı
              //threshold: 0.5,
              //intersectionThreshold: 0.2, // Görünürlük sınırı
              //class: 'is-in-view',
              offset: ["-30%","-30%"]
	    });
        lenis.on('scroll', (e) => {
		    scroll.update();
		});
    }*/
}